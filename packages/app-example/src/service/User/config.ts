import { ServiceInfoType } from '@nobush/core'

export const SERVICE_INFO: ServiceInfoType = {
  serviceName: 'User',
  serviceVersion: '1.0.0',
  serviceDescription: 'service which handles all user related information',
}
